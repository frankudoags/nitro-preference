///
/// HybridPreferenceSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <NitroModules/Null.hpp>
#include <string>
#include <variant>
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::nitropreferences {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Preference`
   * Inherit this class to create instances of `HybridPreferenceSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPreference: public HybridPreferenceSpec {
   * public:
   *   HybridPreference(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPreferenceSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPreferenceSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPreferenceSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::variant<nitro::NullType, std::string>>> getString(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<void>> setString(const std::string& key, const std::string& value) = 0;
      virtual std::shared_ptr<Promise<std::variant<nitro::NullType, double>>> getNumber(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<void>> setNumber(const std::string& key, double value) = 0;
      virtual std::shared_ptr<Promise<std::variant<nitro::NullType, bool>>> getBool(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<void>> setBool(const std::string& key, bool value) = 0;
      virtual std::shared_ptr<Promise<void>> remove(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<void>> clear() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Preference";
  };

} // namespace margelo::nitro::nitropreferences
